# Public site URL (used for canonical URLs, emails, etc.)
NEXT_PUBLIC_SITE_URL="https://moosematrix.com"

# Database (SQLite now; swap to Postgres later)
DATABASE_URL="file:./dev.db"

# Auth (when we add Auth.js)
AUTH_SECRET=""
AUTH_URL="https://moosematrix.com"

# Email provider (newsletter, password reset)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
FROM_EMAIL="no-reply@moosematrix.com"

# Stripe (store)
STRIPE_PUBLIC_KEY=""
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
